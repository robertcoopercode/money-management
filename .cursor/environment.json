{
  "start": "sudo apt-get update && sudo apt-get install -y postgresql postgresql-client && sudo service postgresql start && sudo -u postgres psql -c \"ALTER USER postgres PASSWORD 'postgres';\" && sudo -u postgres psql -c \"CREATE DATABASE money_management;\" 2>/dev/null; cp -n .env.example .env",
  "install": "pnpm install && pnpm db:generate && pnpm db:migrate && pnpm db:seed",
  "terminals": [
    {
      "name": "API Server",
      "command": "pnpm dev:api"
    },
    {
      "name": "Web Dev Server",
      "command": "pnpm dev:web"
    }
  ]
}

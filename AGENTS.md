# AGENTS.md

## Cursor Cloud specific instructions

This is a greenfield "money-management" repository. As of initial setup, the repo contains only a `README.md` — no source code, dependencies, build system, or services exist yet.

- **No tech stack has been chosen.** There is no `package.json`, `requirements.txt`, `Makefile`, `Dockerfile`, or any other configuration.
- **No services to run.** There are no backend, frontend, or database services defined.
- **No lint, test, or build commands.** These will need to be configured once a tech stack is selected.

Once code is added, update this file with relevant development instructions (how to install deps, run services, lint, test, build).
